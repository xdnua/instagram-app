import 'package:flutter/material.dart';

class FavoriteFollowing extends StatelessWidget {
  const FavoriteFollowing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Favorite Following');
  }
}
